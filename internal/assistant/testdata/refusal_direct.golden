⚠️ No machine in your allowed tiers (<code>local</code>) is reachable right now — <code>monster</code> and <code>5090</code> were unavailable. This conversation is limited to that tier, so I won't send it anywhere else. Wake one of them and ask again.