⚠️ No machine in the household's allowed tiers (<code>local</code>, <code>cloud</code>) is reachable right now — <code>monster</code>, <code>5090</code> and <code>openrouter</code> were unavailable. This conversation is limited to those tiers, so I won't send it anywhere else. Wake one of them and ask again.