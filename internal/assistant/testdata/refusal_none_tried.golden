⚠️ No machine in your allowed tiers (<code>local</code>) is reachable right now — I found no endpoints to try. This conversation is limited to that tier, so I won't send it anywhere else. Wake one of them and ask again.